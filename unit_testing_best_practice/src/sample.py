def add_one(x: float) -> float:
    """Add one to the input number.

    Args:
        x (float): The number to which 1 will be added.

    Returns:
        float: The result of adding 1 to the input number.
    """
    return x + 1